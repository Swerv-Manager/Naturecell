{% comment %} Simpel sidehero: eyebrow, overskrift, intro. Bruges paa undersider. {% endcomment %}

{%- style -%}
  .nc-pagehero { padding: clamp(36px, 5vw, 64px) 0 clamp(10px, 2vw, 20px); }
  .nc-pagehero__inner { max-width: 70ch; }
  .nc-pagehero--center .nc-pagehero__inner { margin: 0 auto; text-align: center; }
{%- endstyle -%}

<div class="nc-pagehero {% if section.settings.center %}nc-pagehero--center{% endif %}">
  <div class="nc-container">
    <div class="nc-pagehero__inner">
      {%- if section.settings.eyebrow != blank -%}
        <p class="nc-eyebrow">{{ section.settings.eyebrow }}</p>
      {%- endif -%}
      <h1>{{ section.settings.heading | default: page.title }}</h1>
      {%- if section.settings.intro != blank -%}
        <p>{{ section.settings.intro }}</p>
      {%- endif -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Side-hero",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Overlinje" },
    { "type": "text", "id": "heading", "label": "Overskrift", "info": "Tom = sidens titel" },
    { "type": "textarea", "id": "intro", "label": "Intro" },
    { "type": "checkbox", "id": "center", "label": "Centrér", "default": false }
  ],
  "presets": [ { "name": "Side-hero" } ]
}
{% endschema %}
