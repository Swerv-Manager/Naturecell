{% comment %} 404, skelet. {% endcomment %}

<div class="nc-section">
  <div class="nc-container" style="text-align: center; max-width: 620px;">
    <p class="nc-eyebrow">{{ 'general.error_404' | t }}</p>
    <h1>{{ 'general.not_found_title' | t }}</h1>
    <p>Den side du leder efter, er flyttet eller findes ikke længere.</p>
    <a class="nc-btn nc-btn--primary" href="{{ routes.all_products_collection_url }}">{{ 'general.view_all_products' | t }}</a>
  </div>
</div>

{% schema %}
{
  "name": "404",
  "tag": "section",
  "settings": []
}
{% endschema %}
